@using TakeTripAsp.Application.Features.CityFeatures.CityDtos;
@model CreateCityDto

@{
    ViewData["Title"] = "Створити";
    Layout = "_TakeATripAdminLayout";
}

<h1>Додати</h1>

<h4>Місто</h4>
<hr />
<div class="row ml-3">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CityName" class="control-label">Введіть назву міста</label>
                <input asp-for="CityName" class="form-control" style="width: 500px;" />
                <span asp-validation-for="CityName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CountryId" class="control-label">Виберіть країну</label>
                <select asp-for="CountryId" asp-items="@(ViewBag.Countries != null ? new SelectList(ViewBag.Countries, "Value", "Text") : null)" class="form-control">
                    <option value="">Виберіть країну</option>
                </select>
                <span asp-validation-for="CountryId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Додати" class="btn btn-outline-success btn-fw" />
            </div>
        </form>
    </div>
</div>

<div class="mb-3">
    <a asp-action="Index" class="btn btn-outline-info btn-fw">Повернутись до списку</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}



